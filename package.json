{
  "name": "quill-delta-to-slack",
  "version": "0.7.0",
  "public": true,
  "description": "Convert from Quill Delta to Slack",
  "main": "index.js",
  "engines": {
    "node": ">=6.4.0"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "mocha \"tests/**/*.spec.js\" --reporter progress --watch",
    "pretty": "find ./**/*.js | xargs prettier --write --no-semi --print-width 80 --single-quote --trailing-comma es5"
  },
  "keywords": [
    "quill",
    "delta",
    "markdown",
    "slack"
  ],
  "author": "Bart Visscher, Slite Team, rdesmartin, Sebastian Frysztak, Nicolás Torres",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^5.12.0",
    "eslint-plugin-node": "^8.0.1",
    "jest": "^23.6.0",
    "prettier": "^1.15.3"
  },
  "dependencies": {
    "lodash": "^4.16.4"
  }
}
